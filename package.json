{
  "name": "redux-effects-location",
  "description": "redux-effects middleware for dealing with location/url",
  "repository": "git://github.com/redux-effects/redux-effects-location.git",
  "version": "0.1.5",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "rm -rf lib && babel src --out-dir lib",
    "postpublish": "rm -rf lib"
  },
  "dependencies": {
    "bind-url": "^0.1.2"
  },
  "devDependencies": {
    "babel": "^5.8.21",
    "babel-tape-runner": "^1.2.0",
    "standard": "^5.1.0",
    "tape": "^4.2.0"
  }
}
